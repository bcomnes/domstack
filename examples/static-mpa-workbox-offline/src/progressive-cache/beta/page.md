---
title: Progressive cache beta
layout: progressive-cache
---

# Progressive cache beta

This is a second page using the same `progressive-cache` layout from frontmatter.

It demonstrates that a layout-level policy can apply the same offline behavior to multiple pages without repeating a `page.vars.ts` file next to each page.
